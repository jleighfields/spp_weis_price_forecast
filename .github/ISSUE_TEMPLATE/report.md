---
name: Report
about: Report a bug or request a change
title: ''
labels: ''
---

## What happened

## Expected

## Steps to reproduce
<!-- Name the node, the market (DA/RT) and the forecast date if the report is
     about the app or a forecast; those three pin down which slice of data was
     being read. -->

## Environment
<!-- OS, Python version, package or commit version. Say whether torch was
     installed: a plain `uv sync` installs it, but CI deliberately does not
     (`--no-install-package`, see .github/workflows/test.yml), so anything
     touching src/modeling.py behaves differently on a torch-free install
     than on the GB10 box. -->
